# Update <REGISTRY> <NAMESPACE> values before use
apiVersion: apps/v1
kind: Deployment
metadata:
  name: iot-mediciones-impacient
  labels:
    app: iot-mediciones-impacient
spec:
  replicas: 1
  selector:
    matchLabels:
      app: iot-mediciones-impacient
  template:
    metadata:
      labels:
        app: iot-mediciones-impacient
    spec:
      containers:
      - name: iot-mediciones-impacient
        image: <REGISTRY>/<NAMESPACE>/myapp:v1.0.0
        ports:
        - containerPort: 8000
        imagePullPolicy: Always
        env:
        - name: CLOUDANT_URL
          valueFrom:
            secretKeyRef:
              name: cloudant
              key: url
              optional: true
        - name: CLOUDANT_USERNAME
          valueFrom:
            secretKeyRef:
              name: cloudant
              key: username
              optional: true
        - name: CLOUDANT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: cloudant
              key: password
              optional: true

